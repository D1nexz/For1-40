x = float(input("Введите X: "))
n = int(input("Введите N (> 0): "))

res = 1.0
factorial = 1.0
power = 1.0
sign = 1.0

for i in range(1, 2 * n + 1):
  factorial *= i
  power *= x
  if i % 2 == 0:
    sign = -sign
    res += sign * (power / factorial)

print("Результат:", res)
